spring:
  application:
    name: automessage

  profiles:
    active: test
    include: api
  datasource:
    hikari:
      jdbc-url: jdbc:h2:tcp://localhost/~/sms;MODE=MYSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
#      username: root
#      password: ${DB_PASSWORD}
#      driver-class-name: org.mariadb.jdbc.Driver
#      url: jdbc:mariadb://${SERVER_URL}/khanTest

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 10
  h2:
    console:
      enabled: true

logging.level:
  org.hibernate.SQL: debug